<div class="card my-3">
  <div class="card-header">
    <h2 class="h5">
      <%= dgettext("items", "Edit item") %>
    </h2>
  </div>

  <div class="card-body">
    <%= render "_form.html", Map.put(assigns, :action, Routes.item_path(@conn, :update, @item)) %>
  </div>
</div>

<div class="mt-3">
  <%= link dgettext("items", "Back"), to: Routes.item_path(@conn, :index) %>
</div>
